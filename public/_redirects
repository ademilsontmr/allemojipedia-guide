# Serve fresh emoji favicon (bypasses stale CDN cache on /favicon.ico)
/favicon.ico /favicon-48x48.png 200

# Force HTTPS and non-www
http://allemojipedia.com/* https://allemojipedia.com/:splat 301!
http://www.allemojipedia.com/* https://allemojipedia.com/:splat 301!
https://www.allemojipedia.com/* https://allemojipedia.com/:splat 301!

# Do NOT add Pages Functions middleware for trailing slash — it can take
# the project offline (ERR_CONNECTION_TIMED_OUT). Cloudflare Pages already
# 308s directory URLs to trailing slash; React TrailingSlashRedirect covers SPA.
# Optional: zone Redirect Rule in the Cloudflare dashboard (see cloudflare-pages.md).
# Do NOT add a catch-all SPA fallback (/* /index.html 200) — invalid URLs must 404.
